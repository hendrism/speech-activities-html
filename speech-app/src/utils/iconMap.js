import { BookOpen, Users, MessageCircle, Brain, PenTool } from 'lucide-react';

export const iconMap = {
    'BookOpen': BookOpen,
    'Users': Users,
    'MessageCircle': MessageCircle,
    'Brain': Brain,
    'PenTool': PenTool
};
